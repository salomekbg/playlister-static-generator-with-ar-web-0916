<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Playlister Static Generator with Ar | Individual Genre</title>
  </head>
  <body>
    <h1>Playlister Static Generator with Ar | Individual Genre</h1>
    <h2><a href="/genres/<%= @genre.to_slug%>.html"<%= @genre.name %>:</a></h2>
    <ul>
    <% @genre.artists.each do |artist| %>
    <% @genre.songs.each do |song| %>
      <li><h4>
        <a href="/artists/<%= artist.to_slug %>.html"><%= artist.name %> - <%= song.name %> - <%= @genre.name %></a>
        <a href="/songs/<%= song.to_slug %>.html"><%= artist.name %> - <%= song.name %> - <%= @genre.name %></a>
      </h4></li>
    <% end %>
    <% end %>
    </ul>
  </body>
</html>
