<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Playlister Static Generator with Ar | Individual Artist</title>
  </head>
  <body>
    <h1>Playlister Static Generator with Ar | Individual Artist</h1>
    <h2><a href="/artists/<%= @artist.to_slug%>.html"><%= @artist.name %></a></h2>
    <ul>
      <% @artist.songs.each do |song| %>
      <% @artist.genres.each do |genre| %>
        <li><h4><a href="/songs/<%= song.to_slug %>.html"><%= @artist.name %> - <%= song.name %> - <%= genre.name %></a></h4></li>
        <li><h4><a href="/genres/<%= genre.to_slug %>.html"><%= @artist.name %> - <%= song.name %> - <%= genre.name %></a></h4></li>
      <% end %>
      <% end %>
    </ul>
  </body>
</html>
