<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Playlister Static Generator with Ar | Individual Song</title>
  </head>
  <body>
    <h1>Playlister Static Generator with Ar | Individual Song</h1>
    <h4><a href="/songs?id=<%= @song.id %>"><%= @song.artist.name %> - <%= @song.name %> - <%= @song.genre.name %></a></h4>
  </body>
</html>
